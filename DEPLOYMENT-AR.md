# 🚀 دليل رفع المشروع أونلاين على Railway

دليل شامل بالعربية لرفع منصة المتاجر المتعددة على Railway.

## 📋 المتطلبات الأساسية

1. حساب GitHub (مجاني)
2. حساب Railway (مجاني - [railway.app](https://railway.app))
3. مشروعك موجود على GitHub

---

## الخطوة 1️⃣: رفع الكود على GitHub

### إذا لم يكن المشروع على GitHub بعد:

```bash
# افتح Terminal في مجلد المشروع
cd profesional-web-business

# ابدأ Git repository
git init

# أضف جميع الملفات
git add .

# احفظ التغييرات
git commit -m "Initial commit - Multi-tenant store platform"

# اربط المشروع بـ GitHub
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# ارفع الكود
git push -u origin main
```

### إذا كان المشروع موجود على GitHub:

```bash
git add .
git commit -m "Ready for deployment"
git push
```

---

## الخطوة 2️⃣: إنشاء حساب على Railway

1. اذهب إلى [railway.app](https://railway.app)
2. اضغط على **"Start a New Project"** أو **"Login"**
3. سجل دخول بحساب GitHub (أسهل طريقة)
4. وافق على الصلاحيات المطلوبة

---

## الخطوة 3️⃣: إنشاء مشروع جديد على Railway

1. في لوحة التحكم، اضغط **"New Project"**
2. اختر **"Deploy from GitHub repo"**
3. اختر المستودع (Repository) الخاص بك
4. Railway سيكتشف تلقائياً أنه مشروع Next.js

---

## الخطوة 4️⃣: إضافة قاعدة بيانات PostgreSQL

1. في صفحة المشروع، اضغط **"New"** أو **"+"**
2. اختر **"Database"**
3. اختر **"Add PostgreSQL"**
4. انتظر حتى يتم إنشاء قاعدة البيانات (دقيقة أو دقيقتين)

---

## الخطوة 5️⃣: الحصول على رابط قاعدة البيانات

1. اضغط على خدمة PostgreSQL التي أنشأتها
2. اذهب إلى تبويب **"Variables"** أو **"Connect"**
3. انسخ قيمة `DATABASE_URL` - ستبدو هكذا:
   ```
   postgresql://postgres:password@hostname:port/railway
   ```
4. **احفظ هذا الرابط** - ستحتاجه في الخطوة التالية

---

## الخطوة 6️⃣: إعداد المتغيرات البيئية (Environment Variables)

1. في صفحة المشروع الرئيسية، اضغط على خدمة Next.js (الخدمة التي تحتوي على الكود)
2. اذهب إلى تبويب **"Variables"**
3. اضغط **"New Variable"** وأضف المتغيرات التالية:

### المتغيرات المطلوبة:

#### 1. DATABASE_URL
- **الاسم**: `DATABASE_URL`
- **القيمة**: الصق رابط قاعدة البيانات من الخطوة 5
- **مثال**: `postgresql://postgres:xxxxx@containers-us-west-xxx.railway.app:5432/railway`

#### 2. JWT_SECRET
- **الاسم**: `JWT_SECRET`
- **القيمة**: أنشئ مفتاح سري قوي
  - على Windows PowerShell: `[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))`
  - أو استخدم موقع: [randomkeygen.com](https://randomkeygen.com)
  - أو استخدم أي نص عشوائي طويل (مثل: `my-super-secret-jwt-key-2024-production-xyz123`)

#### 3. APP_URL
- **الاسم**: `APP_URL`
- **القيمة**: سيكون رابط مشروعك على Railway
  - في البداية استخدم: `https://YOUR_PROJECT_NAME.up.railway.app`
  - أو احصل على الرابط من تبويب **"Settings"** → **"Domains"**

#### 4. STORAGE_PATH
- **الاسم**: `STORAGE_PATH`
- **القيمة**: `/app/storage`

### المتغيرات الاختيارية:

#### MAX_FILE_SIZE_MB
- **الاسم**: `MAX_FILE_SIZE_MB`
- **القيمة**: `10`

#### MAX_IMAGE_SIZE_MB
- **الاسم**: `MAX_IMAGE_SIZE_MB`
- **القيمة**: `5`

---

## الخطوة 7️⃣: الانتظار حتى اكتمال البناء (Build)

1. Railway سيبدأ تلقائياً في:
   - تثبيت المكتبات (`npm install`)
   - توليد Prisma Client (`prisma generate`)
   - تشغيل Migrations (`prisma migrate deploy`)
   - بناء المشروع (`npm run build`)

2. راقب التقدم من تبويب **"Deployments"**
3. انتظر حتى ترى **"Build Succeeded"** أو **"Deploy Succeeded"**

---

## الخطوة 8️⃣: الحصول على رابط المشروع

1. في صفحة المشروع، اضغط على خدمة Next.js
2. اذهب إلى تبويب **"Settings"**
3. ابحث عن **"Domains"** أو **"Generate Domain"**
4. اضغط **"Generate Domain"** إذا لم يكن موجوداً
5. انسخ الرابط (مثل: `https://your-project.up.railway.app`)

---

## الخطوة 9️⃣: تجربة المشروع

1. افتح الرابط في المتصفح
2. يجب أن ترى صفحة تسجيل الدخول
3. سجل حساب جديد
4. أنشئ متجرك الأول
5. ابدأ في إضافة المنتجات!

---

## 🔧 حل المشاكل الشائعة

### المشكلة: خطأ في الاتصال بقاعدة البيانات

**الحل**:
- تأكد من أن `DATABASE_URL` صحيح
- تأكد من أن خدمة PostgreSQL تعمل (Status: Active)
- تحقق من أن `DATABASE_URL` يحتوي على `?sslmode=require` في النهاية

### المشكلة: خطأ في البناء (Build Failed)

**الحل**:
1. افتح تبويب **"Deployments"**
2. اضغط على آخر عملية بناء
3. اقرأ رسالة الخطأ
4. الأخطاء الشائعة:
   - متغيرات بيئية مفقودة → أضفها من تبويب Variables
   - خطأ في Prisma → تحقق من `schema.prisma`
   - خطأ في الكود → راجع الأخطاء في السجلات (Logs)

### المشكلة: الموقع لا يعمل بعد البناء

**الحل**:
1. تحقق من أن البناء اكتمل بنجاح
2. تحقق من السجلات (Logs) لمعرفة الأخطاء
3. تأكد من أن جميع المتغيرات البيئية موجودة
4. جرب إعادة البناء (Redeploy)

### المشكلة: الصور لا تظهر

**الحل**:
- تأكد من أن `STORAGE_PATH` مضبوط على `/app/storage`
- في Railway، الملفات المحلية قد تُحذف عند إعادة البناء
- للحل الدائم، استخدم S3 أو خدمة تخزين سحابية

---

## 📝 ملاحظات مهمة

### ⚠️ الملفات المحلية (Local Storage)

- الملفات المحفوظة في `/app/storage` قد تُحذف عند إعادة البناء
- للحل الدائم، استخدم:
  - AWS S3
  - Cloudinary
  - أو أي خدمة تخزين سحابية أخرى

### 🔐 الأمان

- **لا تشارك** `JWT_SECRET` أو `DATABASE_URL` مع أحد
- استخدم مفتاح `JWT_SECRET` قوي وطويل
- في الإنتاج، استخدم HTTPS (Railway يوفرها تلقائياً)

### 💾 قاعدة البيانات

- Railway يوفر نسخ احتياطي تلقائي
- يمكنك تصدير البيانات من Railway Dashboard
- للنسخ الاحتياطي اليدوي، استخدم `pg_dump`

---

## 🎯 الخطوات السريعة (Quick Reference)

```bash
# 1. ارفع الكود على GitHub
git add .
git commit -m "Ready for deployment"
git push

# 2. على Railway:
#    - أنشئ مشروع جديد
#    - أضف PostgreSQL
#    - أضف Environment Variables:
#      * DATABASE_URL (من PostgreSQL)
#      * JWT_SECRET (مفتاح عشوائي)
#      * APP_URL (رابط Railway)
#      * STORAGE_PATH = /app/storage

# 3. انتظر اكتمال البناء

# 4. افتح الرابط وجرب!
```

---

## 📞 الدعم

إذا واجهت مشاكل:

1. **تحقق من السجلات (Logs)**:
   - في Railway Dashboard
   - تبويب **"Deployments"** → اختر آخر عملية → **"View Logs"**

2. **تحقق من المتغيرات البيئية**:
   - تأكد من وجود جميع المتغيرات المطلوبة
   - تأكد من صحة القيم

3. **تحقق من قاعدة البيانات**:
   - تأكد من أن PostgreSQL يعمل
   - تحقق من `DATABASE_URL`

4. **راجع ملف README.md**:
   - يحتوي على معلومات إضافية

---

## ✅ قائمة التحقق النهائية

قبل أن تبدأ:

- [ ] الكود موجود على GitHub
- [ ] حساب Railway جاهز
- [ ] قاعدة بيانات PostgreSQL تم إنشاؤها
- [ ] جميع Environment Variables موجودة
- [ ] البناء اكتمل بنجاح
- [ ] الرابط يعمل

بعد الرفع:

- [ ] يمكنني فتح الموقع
- [ ] يمكنني التسجيل
- [ ] يمكنني إنشاء متجر
- [ ] يمكنني إضافة منتجات
- [ ] المتجر العام يعمل (`/store/[slug]`)

---

**🎉 تهانينا! مشروعك الآن أونلاين!**

إذا احتجت مساعدة إضافية، راجع ملف `DEPLOYMENT.md` أو تواصل مع الدعم.

